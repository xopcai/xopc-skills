# Agent-oriented developer tools

## User outcome

Create durable command-line tools with discoverable operations, stable machine output, safe authentication, and explicit write controls.

## Selected Skill

- `agent-cli-builder` owns the design and implementation of a reusable CLI for a service or repeated local workflow.

## Boundary

It does not own one-off shell scripts, MCP servers, or product-specific connector packaging.
