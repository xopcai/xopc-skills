# Software architecture design

## User outcome

Reshape a module so callers learn a small stable interface while implementation complexity and tests remain local.

## Selected Skill

- `module-design` owns interface depth, seam placement, dependency direction, and migration design for a bounded module.

## Boundary

It does not replace system-wide architecture reviews, domain glossary work, ordinary refactoring, or code review.
