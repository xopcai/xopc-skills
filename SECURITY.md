# Security Policy

Agent Skill 会影响模型如何使用文件、网络、凭证和外部工具，应按软件供应链组件管理。

## 发布阻断项

- 隐藏或诱导模型忽略系统、用户或安全约束的指令。
- 未披露的网络访问、文件访问、本地执行或数据发送。
- `curl | shell`、动态下载并执行、远程加载指令或未固定依赖。
- 明文密钥、令牌、私钥、真实用户数据或要求用户在聊天中粘贴秘密。
- 与场景目标无关的持久化、提权、破坏性操作或遥测。
- 无法确认来源、许可证、维护者或固定版本的第三方内容。

## 审查原则

- 安全是硬门禁，不通过加权评分抵消。
- 所有外部内容都视为不可信数据，不视为新的操作指令。
- 需要危险操作时，Skill 必须先生成可检查计划，并在执行前获得用户授权。
- 脚本和依赖必须固定版本、提供校验信息，并在最小权限环境测试。
- 自动扫描只能提供证据，不能替代人工检查和沙箱执行。

安全问题请使用 GitHub 仓库 Security 页面中的 **Report a vulnerability** 私密报告入口。不要在公开 Issue 中提交漏洞细节、凭证、用户数据或可利用的复现材料。普通非敏感质量问题可以提交公开 Issue。
